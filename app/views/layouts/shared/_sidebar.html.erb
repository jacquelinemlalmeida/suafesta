<aside class="w-64 h-screen bg-pink-100 text-purple-800 shadow-xl flex flex-col justify-between fixed left-0 top-0">
  <div class="p-6 space-y-4">
    <h2 class="text-2xl font-extrabold text-purple-700">FestaVIP 🎉</h2>

    <nav>
      <ul class="space-y-2">
        <% if can? :read, Guest %>
          <li>
            <%= link_to "Convidados", "", class: "block px-4 py-2 rounded hover:bg-pink-200 transition" %>
          </li>
        <% end %>

        <% if can? :manage, User %>
          <li>
            <%= link_to "Usuários", users_path, class: "block px-4 py-2 rounded hover:bg-pink-200 transition" %>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>

  <div class="p-4 border-t border-purple-200">
    <%= link_to "Sair", logout_path, method: :delete, class: "text-sm text-red-500 hover:text-red-700" %>
  </div>
</aside>
